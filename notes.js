//  routes --> that i send the request to the controller

// models --> that interact with the database (schema)

// controllers --> that contain the business logic

//middlewares --> that contain the middleware functions ( you make a account before purshing)

// config --> that contain the configuration files (db connection, env variables) ay 7aga 5argia

// DB --> database connection file

// node_modules --> that contain all the installed packages

// services --> that contain the services ( like sending email, sms, payment gateway)

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// installitions

// npm init -y
// npm i express        → for creating the server, handling routes & middleware
// npm i mongoose       → for connecting to MongoDB and working with schemas & models
// npm i dotenv         → for managing environment variables securely from a .env file
// npm i joi            → for validating request data like inputs, emails, passwords
// npm i cookie-parser  → for reading and writing cookies in Express
// npm i cors           → for enabling cross-origin requests, e.g. frontend ↔ backend
// npm i bcryptjs       → for hashing and comparing passwords securely
// npm i morgan         → for logging HTTP requests, useful for debugging
// npm i nodemon --save-dev → for auto-restarting server during development on file changes
// npm i express-async-handler → for handling async errors in Express routes


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

//-- NOTES --

// package.json:

// we change in the package json to type="module" to use import export instead of require module.exports
// we add in the package json "start": "node index.js" to run the server by npm start


// DB :

//we will connnect on atlas to make it online and access it from anywhere not only locally by robomongo or compass

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// how the files work together :

// index.js -->  by48l el ma4ro3 
// mongoose.js --> byconnect lel database 
// routes --> byrbthom be ba3d 
// middlewares --> byhndl el ma4akel 
// services -->  el4rot bet3ty 
// controllers --> law kolo tmm byb3to lel models
// models --> el 4akl beta3 ek bynat fel database 

